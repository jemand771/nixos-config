# hardware

Collection of hardware scan results. Do not modify, put custom options elsewhere.

Regenerate for the current host:

```sh
sudo nixos-generate-config --show-hardware-config --no-filesystems
nix fmt
```

(`--no-filesystems` only for disko hosts, which are currenly none of them)
